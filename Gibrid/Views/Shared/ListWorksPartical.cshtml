@model Works
<div class="row mt-5 mb-2">
          
    <div class="col-md-4">
        <img class="img-thumbnail" style=" border-style: none; " src="data:image/jpeg;base64,@(Convert.ToBase64String(Model.Image))" alt="" />
        <a class="btn btn-sm btn-primary" id="deleteWork" onclick="ReturnListWorks()" asp-controller="Master" asp-action="DeleteWork" asp-route-workid="@Model.Id" asp-route-user="@User.Identity.Name">Удалить</a>
    </div>

</div>